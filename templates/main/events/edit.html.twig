{% extends 'base.html.twig' %}

{% block title %}
    {{ parent() }} - Events - {{ event.name }}
{% endblock title %}

{% block body %}
    <div class="container mt-4">
        {{ form_start(form) }}
            {{ form_rest(form) }}
        {{ form_end(form) }}
    </div>
{% endblock body %}