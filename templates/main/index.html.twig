{% extends 'base.html.twig' %}

{% block title %}
    {{ parent() }} - Home
{% endblock title %}

{% block body %}
    <div class="container">
    {% if navigation %}
        {% include 'includes/ui/navigation.html.twig' %}
    {% endif %}

    <div class="row">
        <div class="col-12 col-md-8 mb-4">
            <h4 class="mb-3 text-center">Your upcoming events</h4>

            <table class="table-fixed w-100 text-left">
                <thead>
                    <tr class="title-font tracking-wider font-medium text-gray-900 text-sm bg-gray-200 text-center">
                        <th class="px-4 py-3 rounded-tl rounded-bl">Date</th>
                        <th class="px-4 py-3">Time</th>
                        <th class="px-4 py-3 rounded-tr rounded-br">Event</th>
                    </tr>
                </thead>
                <tbody>
                    {% for event in app.user.events %}
                        <tr>
                            <td class="px-4 py-3">
                                {{ event.startDateTime|date('d/m/Y') }}
                            </td>
                            <td class="px-4 py-3">
                                {{ event.startDateTime|date('H:i:s') }} - {{ event.endDateTime|date('H:i:s') }}
                            </td>
                            <td class="px-4 py-3">
                                <a href="{{ path('event_view', {'id': event.id, 'name': event.name}) }}">{{ event.name }}</a>
                            </td>
                        </tr>
                    {% else %}
                        <p>You haven't signed up for any events yet. Consider looking <a>here</a>.</p>
                    {% endfor %}
                </tbody>
            </table>
        </div>

        <div class="col-12 col-md-4">
            <div class="flex flex-wrap">
                <div class="h-full border-2 border-gray-200 rounded-lg overflow-hidden">
                    <img class="lg:h-48 md:h-36 w-full object-cover object-center"
                         src="https://dummyimage.com/720x400" alt="blog">
                    <div class="p-6 border border-gray-500">
                        <h1 class="title-font text-lg font-medium text-gray-900 mb-3">
                            The Catalyzer
                        </h1>
                        <p class="leading-relaxed mb-3">
                            Photo booth fam kinfolk cold-pressed sriracha leggings jianbing
                            microdosing tousled waistcoat.
                        </p>
                        <div class="flex items-center flex-wrap ">
                            <a class="text-indigo-500 inline-flex items-center md:mb-2 lg:mb-0">
                                Learn More
                                <svg class="w-4 h-4 ml-2" viewBox="0 0 24 24"
                                     stroke="currentColor" stroke-width="2" fill="none"
                                     stroke-linecap="round" stroke-linejoin="round">
                                    <path d="M5 12h14"></path>
                                    <path d="M12 5l7 7-7 7"></path>
                                </svg>
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock body %}