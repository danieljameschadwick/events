<nav class="w-full fixed bg-gray-dark z-overlay min-h-60">
    <div class="container flex items-center justify-between flex-wrap p-6">
        <div class="flex items-center flex-no-shrink mr-3">
            <a class="flex flex-row fill-current text-theme-primary hover:no-underline hover:text-white"
               href="{{ path('app_index') }}">
                <svg class="h-8 w-8 mr-3" width="54" height="54" viewBox="0 0 54 54" xmlns="http://www.w3.org/2000/svg">
                    <path d="M13.5 22.1c1.8-7.2 6.3-10.8 13.5-10.8 10.8 0 12.15 8.1 17.55 9.45 3.6.9 6.75-.45 9.45-4.05-1.8 7.2-6.3 10.8-13.5 10.8-10.8 0-12.15-8.1-17.55-9.45-3.6-.9-6.75.45-9.45 4.05zM0 38.3c1.8-7.2 6.3-10.8 13.5-10.8 10.8 0 12.15 8.1 17.55 9.45 3.6.9 6.75-.45 9.45-4.05-1.8 7.2-6.3 10.8-13.5 10.8-10.8 0-12.15-8.1-17.55-9.45-3.6-.9-6.75.45-9.45 4.05z"/>
                </svg>

                <span class="font-semibold text-xl tracking-tight">events</span>
            </a>
        </div>

        <div class="block hidden">
            <button class="flex items-center px-3 py-2 border rounded text-teal-lighter border-teal-light hover:text-white hover:border-white">
                <svg class="h-3 w-3" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><title>Menu</title><path d="M0 3h20v2H0V3zm0 6h20v2H0V9zm0 6h20v2H0v-2z"/></svg>
            </button>
        </div>

        <div class="w-auto flex-grow lg:flex lg:items-center">
            <div class="text-sm lg:flex-grow">
                {% if app.user.uuid is defined %}
                    <a href="{{ path('app_homepage') }}" class="link--primary inline-block hover:text-white mx-2 text-center">
                        Home
                    </a>
                {% endif %}
                <a href="{{ path('event_listing') }}" class="link--primary inline-block hover:text-white mx-2 text-center">
                    Events
                </a>
                <a href="{{ path('calendar_index') }}" class="link--primary inline-block hover:text-white mx-2 text-center">
                    Calendar
                </a>
                <a href="{{ path('news_listing') }}" class="link--primary inline-block hover:text-white mx-2 text-center">
                    News
                </a>
            </div>
        </div>

        <div>
            {% if app.user %}
                {{ include('components/ui/user_dropdown.html.twig', {
                    'panelItems': [
                        {
                            'name': 'Profile',
                            'routeName': 'app_user_view',
                            'routeParams': {
                                'userName': app.user.username
                            }
                        },
                        {
                            'name': 'Settings',
                            'routeName': 'app_user_settings',
                            'routeParams': {
                                'userName': app.user.username
                            }
                        },
                        {
                            'name': 'Logout',
                            'routeName': 'app_logout',
                            'additionalClass': 'bg-red-700 text-white'
                        }
                    ]
                }) }}
            {% else %}
                <a class="link--primary" href="{{ url('app_login') }}">Login</a>
            {% endif %}
        </div>
    </div>
</nav>